# SPDX-FileCopyrightText: 2026 Alex Brandt <alunduil@gmail.com>
# SPDX-License-Identifier: MIT

StylesPath = .vale/styles
MinAlertLevel = suggestion
# Pinned to release URLs, not bare names. A bare name resolves to
# releases/latest, so the rule set shifts under the gate with no commit
# here — Microsoft v0.15.1 added UIVerbs, Militaristic, SentenceLength,
# and Uppercase that way. Renovate tracks these URLs.
Packages = https://github.com/errata-ai/Microsoft/releases/download/v0.15.1/Microsoft.zip, https://github.com/errata-ai/proselint/releases/download/v0.3.4/proselint.zip, https://github.com/errata-ai/alex/releases/download/v0.2.3/alex.zip
Vocab = alunduil-infrastructure

# Human-read prose: README and the Diátaxis how-tos.
[{README,docs/**/*}.md]
BasedOnStyles = Vale, Microsoft, proselint, alex

proselint.Cliches = NO   # overlaps Microsoft.Terms
proselint.Spelling = NO  # Vale.Spelling owns this

# Wants tight em-dashes; spaced ' — ' is this repo's style.
Microsoft.Dashes = NO
# Prefers 'address' over 'URL', but the how-tos name literal form fields
# ('Homepage URL', 'Callback URL') the operator has to find in a UI.
Microsoft.GeneralURL = NO
# Demands a definition for DNS, TTL, OIDC, SCALE. The audience already
# administers these systems.
Microsoft.Acronyms = NO
# Semicolons are deliberate here, not a sentence-splitting failure.
Microsoft.Semicolon = NO
# 'Verify your use of X with the A-Z word list' carries no fix to apply.
Microsoft.Vocab = NO

# Claude-read instructions. Optimised for tokens, not readability, so the
# rules that inflate prose (sentence case, contractions, passive voice,
# 'e.g.' expansion) are absent. Enabled instead: correctness, inclusive
# language, and the rules that push toward fewer words.
[{CLAUDE,.claude/**/*}.md]
BasedOnStyles = Vale, alex

Microsoft.Adverbs = YES
Microsoft.Avoid = YES
Microsoft.Terms = YES
Microsoft.Wordiness = YES
